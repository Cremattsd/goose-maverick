{
  "name": "goose-maverick",
  "version": "1.0.0",
  "scripts": {
    "build:css": "mkdir -p static && npx tailwindcss -i ./rc/input.css -o ./static/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
