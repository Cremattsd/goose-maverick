Flask==2.3.3  # Upgraded for security and async support
Flask-Cors==4.0.0  # Updated for compatibility
Flask-SocketIO==5.3.6  # Same, solid
gunicorn==20.1.0  # Same, good for Render
PyMuPDF==1.20.2  # Same, for PDF parsing
pandas==1.5.0  # Same, for Excel
numpy==1.23.3  # Same, for pandas
pytesseract==0.3.10  # Same, for OCR
Pillow==9.2.0  # Same, for image processing
werkzeug==2.2.2  # Same, for Flask
openai==1.30.1  # Upgraded for GPT-4o
tenacity==8.1.0  # Same, for retries
pdf2image==1.16.0  # Same, for PDF-to-image
exifread==3.0.0  # Same, for geolocation
httpx==0.24.1  # Added for async HTTP
redis==5.0.3  # Added for caching
